import SaliouKoffiAction from "../../components/Partenaire Omisify/SaliouKoffiAction";

export default function SaliouKoffi() {
  const family = {
    fontFamily:
      '"Source Sans Pro", "Helvetica Neue", Helvetica, Arial, sans-serif',
  };
  return (
    <div style={family}>
      <SaliouKoffiAction />
    </div>
  );
}
