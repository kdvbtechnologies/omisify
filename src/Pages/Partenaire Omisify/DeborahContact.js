import DeborahContactAction from "../../components/Partenaire Omisify/DeborahContactAction";

export default function DeborahContact() {
  const family = {
    fontFamily:
      '"Source Sans Pro", "Helvetica Neue", Helvetica, Arial, sans-serif',
  };
  return (
    <div style={family}>
      <DeborahContactAction />
    </div>
  );
}
