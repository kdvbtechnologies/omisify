import WilfriedContactAction from "../../components/Partenaire Omisify/WilfriedContactAction";

export default function WilfriedContact() {
  const family = {
    fontFamily:
      '"Source Sans Pro", "Helvetica Neue", Helvetica, Arial, sans-serif',
  };
  return (
    <div style={family}>
      <WilfriedContactAction />
    </div>
  );
}
