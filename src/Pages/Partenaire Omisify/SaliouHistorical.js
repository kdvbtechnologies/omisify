import SaliouHistoricalAction from "../../components/Partenaire Omisify/SaliouHistoricalAction";

export default function SaliouHistorical() {
  const family = {
    fontFamily:
      '"Source Sans Pro", "Helvetica Neue", Helvetica, Arial, sans-serif',
  };
  return (
    <div style={family}>
      <SaliouHistoricalAction />
    </div>
  );
}
